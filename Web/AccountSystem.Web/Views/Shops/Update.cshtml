@model AccountSystem.Web.Models.ShopViewModel

@{
    ViewBag.Title = "Update a shop";
}

<div>
    <a href="@Url.Content("~/Shops/Index")" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Back</a>
</div>

<div><h3>Update shop</h3></div>

@using (Html.BeginForm("Update", "Shops", FormMethod.Post, new { @class = "form-horizontal form-custome" }))
{
    @Html.AntiForgeryToken()

    <div class="control-group">
        @Html.LabelFor(m => m.Name, new { @class = "control-label" })
        <div class="controls">
            @Html.TextBoxFor(m => m.Name)
            @Html.ValidationMessageFor(m => m.Name)
        </div>
    </div>
    <br />
    <div class="control-group">
        <div class="controls">
            <button type="submit" class="btn btn-primary">Update</button>
            <a href="@Url.Content("~/Shops/Index")" class="btn btn-danger">Cancel</a>
        </div>
    </div>
}